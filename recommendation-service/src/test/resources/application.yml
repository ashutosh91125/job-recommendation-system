spring:
  application:
    name: recommendation-service
    bootstrap-servers: localhost:29092
    consumer:
      group-id: recommendation-test-group
      auto-offset-reset: earliest

eureka:
  client:
    enabled: false

app:
  model:
    path: models/test-recommendation-model.zip
